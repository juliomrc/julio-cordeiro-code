import { makeStyles } from "@material-ui/core/styles";

export const useAnimatedHomepageHeaderStyles = makeStyles(() => {
    return {
        container: {
            width: "100%",
        },
        buttonContainer: {
            marginBottom: "10px",
        },
    };
});
