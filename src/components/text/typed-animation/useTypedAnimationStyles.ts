import { makeStyles } from "@material-ui/core";

export const useTypedAnimationStyles = makeStyles(() => {
    return {
        container: {
            overflow: "hidden",
            width: "100%",
        },
    };
});
