import { makeStyles } from "@material-ui/core/styles";

export const useDragAndDropVerticalListStyles = makeStyles(() => {
    return {
        itemMargin: {
            marginBottom: "10px",
        },
    };
});
